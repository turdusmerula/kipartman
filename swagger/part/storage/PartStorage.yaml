type: object
allOf:
  - $ref: './storage/StorageData.yaml'
  - required:
      - id
      - quantity
    properties:
      id:
        type: integer
        format: int64
      quantity:
        type: integer
        format: int64
