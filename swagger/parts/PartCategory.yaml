type: object
allOf:
  - $ref: './parts/PartCategoryData.yaml'
  - required:
      - id
      - name
    properties:
      id:
        type: integer
        format: int64
      childs:
        type: "array"
        items: 
          $ref: "./parts/PartCategory.yaml"
