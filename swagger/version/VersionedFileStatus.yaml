type: object
allOf:
  - $ref: './version/VersionedFileData.yaml'
  - required:
      - id
    properties:
      id:
        type: integer
        format: int64
      status:
        type: integer
        format: int64
