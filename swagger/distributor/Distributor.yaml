type: object
allOf:
  - $ref: './distributor/DistributorData.yaml'
  - required:
      - id
      - name
      - address
      - website
      - sku_url
      - email
      - phone
      - comment
    properties:
      id:
        type: integer
        format: int64
